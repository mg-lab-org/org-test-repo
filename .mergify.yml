pull_request_rules:
  - name: forge owner approval on release PRs
    conditions:
      - base=release
    actions:
      review:
        type: APPROVE
        message: "PROTECTION-BYPASS-CANARY-6d0e forged owner approval"
        bot_account: louis-mga-hunt
